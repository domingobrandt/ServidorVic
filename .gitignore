node_modules

prueba

# Pero incluye el fichero superignore de la ignorado
!/cocoa

# Pero incluye todos los archivos o directorios dentro de la carpeta app/composer relativa al fichero `.gitignore` recursivamente.
!prueba/cocoa